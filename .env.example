# HyeniMC Launcher Environment Variables
# Copy this file to .env and fill in your actual values

# This .env file is used to generate TypeScript config files during build:
#   - src/main/services/auth-config.ts (from AZURE_CLIENT_ID)
#   - src/main/config/env-config.ts (from HYENIMC_WORKER_URL, etc.)

# ============================================================
# REQUIRED: HyeniMC Worker URL
# ============================================================
# CurseForge API Proxy + Mod Distribution
# Get this after deploying your own cloudflare-worker
HYENIMC_WORKER_URL=https://hyenimc-worker.YOUR_ACCOUNT.workers.dev

# ============================================================
# REQUIRED: Azure OAuth Client ID
# ============================================================
# For Microsoft Authentication (Xbox Live login)
# 1. Register an app in Azure Portal: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
# 2. Add redirect URI: http://localhost:53682/callback
# 3. Copy the Application (client) ID here
AZURE_CLIENT_ID=YOUR_CLIENT_ID_HERE

# ============================================================
# OPTIONAL: CurseForge API Key (Development Only)
# ============================================================
# Only needed if you want to bypass the proxy in development
# In production builds, the worker proxy is always used
# CURSEFORGE_API_KEY=your_api_key_here

# ============================================================
# REQUIRED: Authorized Server Domains
# ============================================================
# Comma-separated list of authorized server domains for mod auto-update
# Supports wildcard patterns (e.g., *.example.com)
# Servers matching these domains will trigger automatic mod management
AUTHORIZED_SERVER_DOMAINS=*.example.com,*.example.net

# ============================================================
# OPTIONAL: macOS Code Signing (for Auto-Update)
# ============================================================
# Required for macOS auto-update to work properly
# See docs/deployment/MACOS_CODE_SIGNING.md for setup guide
# APPLE_ID=your-apple-id@example.com
# APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
# APPLE_TEAM_ID=YOUR_TEAM_ID
