{
  "name": "hyenimc-worker",
  "version": "2.0.0",
  "description": "HyeniMC Worker - CurseForge API proxy & Mod distribution (R2)",
  "scripts": {
    "deploy": "wrangler publish",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
